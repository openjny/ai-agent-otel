# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: ai-agent-otel
metadata:
  template: ai-agent-otel
hooks:
  postprovision:
    shell: sh
    run: bash infra/hooks/postprovision.sh
